d3.csv("data/Updated_Data.csv").then(function(data) {
    // Spotify Streams to Measures Tree Map
});

class TreeMap {
    /**
     * Class constructor
     * @param {Object} _config
     * @param {Array} _data
     * @param {d3.Scale} _colorScale
     */
    constructor(_config, _data, _colorScale, _dispatcher){
        
    }
}